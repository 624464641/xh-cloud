server:
  port: 10006
eureka:
  instance:
    hostname: xh-config
    prefer-ip-address: true
  client:
    service-url:
      defaultZone :  http://xh1:10000/eureka
spring:
  application:
    name: xh-config
  profiles:
    active: native
    # Mysql
  cloud:
    config:
      server:
        native:
          search-locations: /config-file/
